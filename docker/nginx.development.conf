upstream aquarium {
  server app:3000;
}

server {
  listen 80 default_server;
  location / {
      proxy_pass http://aquarium;
  }
}

upstream s3server {
    server s3:9000;
}

server {
    listen 9000 default_server;
    location / {
        proxy_pass http://s3server;
    }
}