import React from 'react';
import { render, screen } from '@testing-library/react';
import AlertToast from './AlertToast';

describe('AlertToast', () => {
  it.todo('should be visible when open is true');
  it.todo('should clear from screen automatically');
  it.todo('should close on use click');
});
