import React from 'react';
import { render, screen } from '@testing-library/react';
import DropdownMenu from './DropdownMenu';

describe('DropdownMenu', () => {
  it.todo('should be closed at first render');
  it.todo('should open the menu when the button is clicked');
  it.todo('should close when an item is clicked');
});
