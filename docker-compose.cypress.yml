  version: "3.7"
  services:
    e2e:
      image: cypress
      build: ./end2end
      container_name: cypress
      depends_on: 
        - frontend
      environment:
        - CYPRESS_BASE_URL=http://frontend:${FRONTEND_PUBLIC_PORT}
      command: npx cypress run
      volumes:
        - ./end2end:/app
      networks:
        - aquarium_net
