<%= form_for(@announcement) do |f| %>
  <% if @announcement.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@announcement.errors.count, "error") %> prohibited this announcement from being saved:</h2>

      <ul>
      <% @announcement.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <h3>Create Announcement</h3><br/>

  <%= f.label :title %>
  <%= f.text_field :title, placeholder: "Your announcement title" %>

  <%= f.label :message %>
  <%= f.text_area :message, placeholder: "Your announcement message", style: 'width: 100%' %>

  <%= f.label :active %>
  <%= f.check_box :active %>

  <br/>
  <br/>

  <%= f.submit class: 'md-button md-raised md-primary' %>
<% end %>
