<div ng-show="mode == 'definition'" ng-controller="operationTypeDefinitionCtrl as opdef">

  <form>

    <div class="row-fluid">
      <div class="span6">
        <label class="label-bold">Operation Name</label>
        <input type='text' ng-model="current_ot.name" class="input-xlarge">
      </div>
      <div class="span6">
        <button class="btn btn-primary pull-right" ng-click="save_ot(current_ot)">
          <span ng-if="current_ot.id">Update Definition</span>
          <span ng-if="!current_ot.id">Save new Operation Type</span>
        </button>
      </div>
    </div>

    <div class='row-fluid'>
      <div class='span6'>
        <label class="label-bold">
          Inputs
          <a href="#" class="btn btn-mini pull-right" ng-click="add_io('input')">Add</a>
        </label>
        <div ng-include="'io_form'" onLoad="role = 'input'" ng-if="true"></div>
      </div>

      <div class='span6'>
        <label class="label-bold">
          Outputs
          <a href="#" class="btn btn-mini pull-right" ng-click="add_io('output')">Add</a>
        </label>
        <div ng-include="'io_form'" onLoad="role = 'output'" ng-if="true"></div>
        </div>
      </div>

  </form>

  <!-- pre>{{current_ot|json}}</pre -->

</div>