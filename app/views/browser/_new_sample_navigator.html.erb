<div class='project-heading project-expander' ng-click="set_mode('new')">
  <span>New Sample(s)</span>        
</div>

<select ng-model="sample_type_choice" 
        ng-options="st.name for st in sample_types track by st.id" 
        class="form-control">
</select>

<br />

<button class='btn btn-primary' ng-click="new_sample()">New</button>  